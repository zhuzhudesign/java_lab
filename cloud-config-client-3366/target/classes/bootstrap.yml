server:
  port: 3366

spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      name: application
      profile: dev
      uri: http://localhost:3344
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://localhost:7002/eureka

#expore monitored endpoints
management:
  endpoints:
    web:
      exposure:
        include: "*"
